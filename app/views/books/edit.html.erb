<h1>Editing Book</h1>

<% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
    <ul>
        <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
    </ul>
<% end %>



<%= form_with model: @book do |e| %>

    <h3>Title</h3>
    <%= e.text_field :title %>

    <h3>Body</h3>
    <%= e.text_area :body %>

    <p><%= e.submit 'Update Book' %></p>

    <%= link_to "Show", book_path %> |
    <%= link_to "戻る", books_path %>

<% end %>